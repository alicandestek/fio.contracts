add_contract(fio.system fio.pnetwork ${CMAKE_CURRENT_SOURCE_DIR}/fio.pnetwork.cpp)

target_include_directories(fio.pnetwork
        PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}
        ${CMAKE_CURRENT_SOURCE_DIR}/../fio.system/include
        ${CMAKE_CURRENT_SOURCE_DIR}/../
        )


set_target_properties(fio.pnetwork
        PROPERTIES
        RUNTIME_OUTPUT_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}")
